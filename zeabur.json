{
  "build": {
    "command": "composer install --no-dev --optimize-autoloader && php artisan config:cache && php artisan route:cache && php artisan view:cache"
  },
  "start": {
    "command": "php artisan migrate --force && php-fpm -D && nginx -g 'daemon off;'"
  }
}
